---
/**
 * PageLayout — BaseLayout + SiteHeader + main + SiteFooter
 */
import BaseLayout from './BaseLayout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import '../tokens/global.css';
import '../tokens/omni-kit.css';

export interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonical?: string;
  site?: 'sbb' | 'news' | 'learn';
  schemaType?: 'Organization' | 'Article' | 'WebPage';
  currentPath?: string;
  /** Header trong suốt, overlay lên hero full-bleed (dùng cho trang chủ kiểu omni) */
  overlay?: boolean;
  /** Schema JSON-LD riêng của trang, chuyển thẳng xuống BaseLayout. */
  jsonLd?: Record<string, any> | Record<string, any>[];
}

const {
  title,
  description,
  ogImage,
  canonical,
  site = 'sbb',
  schemaType,
  currentPath = '',
  overlay = false,
  jsonLd,
} = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  ogImage={ogImage}
  canonical={canonical}
  site={site}
  schemaType={schemaType}
  jsonLd={jsonLd}
>
  <SiteHeader site={site} currentPath={currentPath} overlay={currentPath === '/' || currentPath === ''} />
  <main id="main-content">
    <slot />
  </main>
  <SiteFooter site={site} />
</BaseLayout>
